name: node_ci_fix_effect
version: "1.0.0"
description: "LLM-routed CI fix effect — diagnose failing CI job, generate patch, run test gate"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_ci_fix_effect: "omnimarket.nodes.node_ci_fix_effect"
capabilities:
  standalone: false
  full_runtime: true
  requires_network: true
  requires_repo: true
  requires_secrets: false
  requires_docker: false
  side_effect_class: effectful
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["ci", "fix", "llm", "effect", "merge-sweep", "phase2"]
pack: "merge-sweep"
display_name: "CI Fix Effect"
node_role: "effect"
