name: node_create_ticket
version: "1.0.0"
description: "Create a single Linear ticket from args, contract file, or plan milestone"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_create_ticket: "omnimarket.nodes.node_create_ticket"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: true
  requires_repo: false
  requires_secrets: true
  requires_docker: false
  side_effect_class: write
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["planning", "linear", "ticket", "create"]
pack: "planning"
display_name: "Create Ticket"
node_role: "effect"
