name: node_seam_parallel_executor
version: "1.0.0"
description: "Deterministic wave executor — parallel task execution with protocol shims and dependency ordering"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_seam_parallel_executor: "omnimarket.nodes.node_seam_parallel_executor"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: effectful
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["orchestrator", "seam", "parallel", "wave-executor"]
pack: orchestration
display_name: Seam Parallel Executor
node_role: orchestrator
