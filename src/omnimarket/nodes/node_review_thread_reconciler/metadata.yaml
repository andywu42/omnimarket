name: node_review_thread_reconciler
version: "1.0.0"
description: "Review thread reconciler — re-opens threads resolved by non-bot actors via GitHub GraphQL"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_review_thread_reconciler: "omnimarket.nodes.node_review_thread_reconciler"
capabilities:
  standalone: false
  full_runtime: true
  requires_network: true
  requires_repo: true
  requires_secrets: true
  requires_docker: false
  side_effect_class: write
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["pr-review", "reconciler", "github", "webhook", "review-bot"]
pack: "ci_lifecycle"
display_name: "Review Thread Reconciler"
node_role: "effect"
