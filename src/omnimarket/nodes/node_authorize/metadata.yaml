name: node_authorize
version: "1.0.0"
description: "Write ModelAgentAuthorizationGrant for /onex:authorize skill"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_authorize: "omnimarket.nodes.node_authorize"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: filesystem_write
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["authorization", "security", "session", "effect"]
pack: "security"
display_name: "Authorize"
node_role: "effect"
