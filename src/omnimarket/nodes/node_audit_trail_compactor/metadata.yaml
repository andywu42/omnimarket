name: node_audit_trail_compactor
version: "1.0.0"
description: "Audit trail compactor — daily rollup of friction + dispatch logs"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_audit_trail_compactor: "omnimarket.nodes.node_audit_trail_compactor"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: file_read + file_write
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "audit", "rollup", "compactor", "friction", "dispatch"]
pack: "build"
display_name: "Audit Trail Compactor"
node_role: "compute"
