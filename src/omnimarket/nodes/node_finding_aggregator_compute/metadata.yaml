name: node_finding_aggregator_compute
version: "1.0.0"
description: "Pure computation: merges findings from N models via weighted-union dedup (Jaccard similarity)"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_finding_aggregator_compute: "omnimarket.nodes.node_finding_aggregator_compute"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "review", "aggregation", "dedup", "llm-workflow"]
