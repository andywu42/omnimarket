name: node_local_review
version: "1.0.0"
description: "Iterative local code review loop — review/fix/commit FSM with convergence"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_local_review: "omnimarket.nodes.node_local_review"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["review", "local", "iteration", "fsm", "code-quality"]
pack: "review"
display_name: "Local Review"
node_role: "fix"
