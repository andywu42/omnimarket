name: node_conflict_hunk_effect
version: "1.0.0"
description: "LLM-routed effect node that resolves merge conflict hunks via patch generation"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_conflict_hunk_effect: "omnimarket.nodes.node_conflict_hunk_effect"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: true
  requires_repo: true
  requires_secrets: false
  requires_docker: false
  side_effect_class: write
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["effect", "merge-sweep", "phase2", "conflict-resolution", "llm"]
pack: "merge-sweep"
display_name: "Conflict Hunk Effect"
node_role: "effect"
