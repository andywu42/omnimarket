name: node_dashboard_sweep
version: "1.0.0"
description: "Full dashboard audit — classify pages, triage problem domains, track fix iterations"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_dashboard_sweep: "omnimarket.nodes.node_dashboard_sweep"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["sweep", "dashboard", "audit", "playwright", "quality"]
pack: "diagnostics"
display_name: "Dashboard Sweep"
node_role: "probe"
