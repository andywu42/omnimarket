name: node_session_compose
version: "0.1.0"
description: "Session compose orchestrator — thin scaffold composing session phases (dry-run + live-dispatch placeholder)"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_session_compose: "omnimarket.nodes.node_session_compose"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["orchestrator", "session", "compose", "scaffold"]
pack: "session"
display_name: "Session Compose"
node_role: "orchestrator"
