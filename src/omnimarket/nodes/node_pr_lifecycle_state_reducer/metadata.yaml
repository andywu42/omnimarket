name: node_pr_lifecycle_state_reducer
version: "1.0.0"
description: "PR lifecycle FSM reducer — pure state machine: (state, event) -> (new_state, intents[])"
omnibase_core_compat: ">=0.39.0,<1.0.0"
pack: pr_lifecycle
node_role: reducer
display_name: "PR State Reducer"
entry_points:
  onex.nodes:
    node_pr_lifecycle_state_reducer: "omnimarket.nodes.node_pr_lifecycle_state_reducer"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["reducer", "fsm", "pr-lifecycle"]
