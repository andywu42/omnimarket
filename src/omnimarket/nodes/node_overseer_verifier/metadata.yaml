name: node_overseer_verifier
version: "1.0.0"
description: "Deterministic verification layer — 5-dimension pure Python gate for overseer model outputs"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_overseer_verifier: "omnimarket.nodes.node_overseer_verifier"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: pure
dependencies:
  - "omnibase_core>=0.39.0"
  - "pydantic>=2.0.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["overseer", "verifier", "compute", "deterministic", "pure", "OMN-8031"]
pack: intelligence
display_name: Overseer Verifier
node_role: probe
