name: node_memory_retrieval_effect
version: "1.0.0"
description: "Retrieves documents from the memory fabric via semantic search for agent context enrichment"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_memory_retrieval_effect: "omnimarket.nodes.node_memory_retrieval_effect"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
  - "omninode-memory>=0.1.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["memory", "retrieval", "semantic", "effect"]
pack: "memory"
display_name: "Memory Retrieval Effect"
node_role: "effect"
