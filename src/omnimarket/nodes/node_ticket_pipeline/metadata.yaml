name: node_ticket_pipeline
version: "1.0.0"
description: "Per-ticket execution pipeline — multi-phase FSM workflow package"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_ticket_pipeline: "omnimarket.nodes.node_ticket_pipeline"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["pipeline", "ticket", "workflow", "fsm", "automation"]
