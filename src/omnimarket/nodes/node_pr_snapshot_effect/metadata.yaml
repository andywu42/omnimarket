name: node_pr_snapshot_effect
version: "1.0.0"
description: "Structured PR scanning effect — scans GitHub repos via gh CLI and produces ModelPRInfo for merge sweep pipeline"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_pr_snapshot_effect: "omnimarket.nodes.node_pr_snapshot_effect"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: true
  requires_repo: false
  requires_secrets: true
  requires_docker: false
  side_effect_class: read
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["effect", "pr", "github", "snapshot", "merge-sweep"]
