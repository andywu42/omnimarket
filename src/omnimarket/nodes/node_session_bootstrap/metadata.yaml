name: node_session_bootstrap
version: "1.0.0"
description: "Overnight session bootstrapper — reads contract, writes snapshot, configures timers"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_session_bootstrap: "omnimarket.nodes.node_session_bootstrap"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: mutating
dependencies:
  - "omnibase_core>=0.39.0"
  - "omnibase_compat"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["bootstrap", "orchestrator", "overnight", "session", "contract"]
pack: "overnight_pipeline"
display_name: "Session Bootstrap"
node_role: "orchestrator"
