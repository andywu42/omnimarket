name: node_runtime_sweep
version: "1.0.0"
description: "Runtime registration and wiring verification — node descriptions, handler wiring, topic symmetry"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_runtime_sweep: "omnimarket.nodes.node_runtime_sweep"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: true
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["sweep", "runtime", "wiring", "contracts", "verification"]
pack: "diagnostics"
display_name: "Runtime Sweep"
node_role: "probe"
