name: node_environment_health_scanner
version: "1.0.0"
description: "Live environment health scanner — emit daemon, hooks, Kafka consumers, containers, projections, entry-points, model endpoints"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_environment_health_scanner: "omnimarket.nodes.node_environment_health_scanner"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: true
  requires_repo: true
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
  - "httpx>=0.27.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["health", "diagnostics", "environment", "scanner", "probe"]
pack: "diagnostics"
display_name: "Environment Health Scanner"
node_role: "probe"
