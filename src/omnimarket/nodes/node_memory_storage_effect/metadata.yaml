name: node_memory_storage_effect
version: "1.0.0"
description: "Stores documents and embeddings into the memory fabric for later retrieval"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_memory_storage_effect: "omnimarket.nodes.node_memory_storage_effect"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: write
dependencies:
  - "omnibase_core>=0.39.0"
  - "omninode-memory>=0.1.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["memory", "storage", "embeddings", "effect"]
pack: "memory"
display_name: "Memory Storage Effect"
node_role: "effect"
