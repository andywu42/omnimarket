name: node_build_loop_orchestrator
version: "1.0.0"
description: "Build loop orchestrator -- composes 6 sub-handlers via FSM reducer"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_build_loop_orchestrator: "omnimarket.nodes.node_build_loop_orchestrator"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: true
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: effectful
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["build-loop", "orchestrator", "fsm", "workflow", "autonomous"]
pack: "build"
display_name: "Build Loop Orchestrator"
node_role: "orchestrator"
