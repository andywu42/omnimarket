name: node_build_loop
version: "1.0.0"
description: "Autonomous build loop — 6-phase FSM workflow package"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_build_loop: "omnimarket.nodes.node_build_loop"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["build-loop", "workflow", "fsm", "orchestrator", "autonomous"]
pack: "build"
display_name: "Build Loop"
node_role: "orchestrator"
