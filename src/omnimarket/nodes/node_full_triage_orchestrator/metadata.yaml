name: node_full_triage_orchestrator
version: "1.0.0"
description: "Unified read-only diagnosis orchestrator — ranks findings across every sweep + 3 new probes"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_full_triage_orchestrator: "omnimarket.nodes.node_full_triage_orchestrator"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["triage", "diagnosis", "sweep", "aggregator", "read-only"]
pack: "diagnostics"
display_name: "Full Triage Orchestrator"
node_role: "orchestrator"
