name: node_polish_task_classifier
version: "1.0.0"
description: "Pure compute: deterministic decision table mapping one polish signal to exactly one task class"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_polish_task_classifier: "omnimarket.nodes.node_polish_task_classifier"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "merge-sweep", "phase2", "classification"]
pack: "merge-sweep"
display_name: "Polish Task Classifier"
node_role: "triage"
