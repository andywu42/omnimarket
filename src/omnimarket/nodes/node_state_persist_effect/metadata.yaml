name: node_state_persist_effect
version: "1.0.0"
description: "State persist effect node — writes ModelStateEnvelope to ProtocolStateStore via protocol DI"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_state_persist_effect: "omnimarket.nodes.node_state_persist_effect"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: state_store_write
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["effect", "state", "persist", "reducer", "pure-reducer"]
pack: "build"
display_name: "State Persist Effect"
node_role: "effect"
